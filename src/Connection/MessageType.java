package Connection;

public enum MessageType {
    TEXT,
    NEW_USER,
    EXIT_USER,
    INFO_FROM_SERVER,
    CHANGE_LOGIN,
    UPDATE_USERLIST,
    CONFIRM_CLIENT_NAME,
    LOGIN_AVAILABLE,
    LOGIN_UNAVAILABLE
}
